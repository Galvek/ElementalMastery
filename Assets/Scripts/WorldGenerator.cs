using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WorldGenerator : MonoBehaviour
{
    public List<Texture2D> BasicFloorTiles { get; set; }
    public List<Texture2D> NatureFloorTiles { get; set; }
    public List<Texture2D> WaterFloorTiles { get; set; }
    public List<Texture2D> FireFloorTiles { get; set; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
